#需要mongodb的支持
```
在mongodb安装目录打开cmd
mongod -dbpath = C:\cx\study\Mongodb\data

再次在mongodb安装目录打开cmd
mongo

```
#安装生成器
`npm install express -g`

# 生成项目
>express -e 2018-6blog

#进入生成目录并安装依赖模块
>cd 2018-6blog` && npm install

#设置环境变量并启动
>SET DEBUG=2018-6blog:* & npm start

#安装bower
>npm install bower -g

#初始化bower
bower init

#添加.bowerrc文件
```
{
    "directory":"./public/lib"
}
```
#安装bootstrap
>bower install bootstrap --save

#安装会话中间件
>npm install express-session connect-mongo --save

#显示提示
>npm install connect-flash

#安装上传模板
npm install multer --save